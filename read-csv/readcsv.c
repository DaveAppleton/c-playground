#include<stdio.h>
#include <string.h>

#define BUFFER_SIZE 1024

struct data { 
    int   col1; 
    char  *col2; 
    float col3; 
}

void ParseCSV(char *filename, data &input ) {
    input = input;

}

int main(void) {
    FILE * pInput;
    char buf[BUFFER_SIZE];

    

}